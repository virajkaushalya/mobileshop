import {Text, View} from "react-native";
import {SafeAreaView} from "react-native-safe-area-context";

const Shop = () => {
    return (
        <SafeAreaView>
            <Text className={'text-foreground'}>This is Shop page</Text>
        </SafeAreaView>
    );
}

export default Shop;